import 'package:flutter/material.dart';

class SpeakingListTestPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Speaking Test'),
      ),
      body: Center(
        child: Text('Speaking Test Content Here'),
      ),
    );
  }
}
