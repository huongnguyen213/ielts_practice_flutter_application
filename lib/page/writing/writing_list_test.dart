import 'package:flutter/material.dart';

class WritingListTestPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Writing Test'),
      ),
      body: Center(
        child: Text('Writing Test Content Here'),
      ),
    );
  }
}
