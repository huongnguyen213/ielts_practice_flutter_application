import 'package:flutter/material.dart';

class ListeningListTestPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Listening Test'),
      ),
      body: Center(
        child: Text('Listening Test Content Here'),
      ),
    );
  }
}
